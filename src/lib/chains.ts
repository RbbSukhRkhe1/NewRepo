import { defineChain } from 'viem';

/** Base Sepolia (defined locally so Vite + rolldown do not traverse `viem/chains` subpaths). */
export const baseSepoliaLocal = defineChain({
  id: 84532,
  name: 'Base Sepolia',
  nativeCurrency: { name: 'Ether', symbol: 'ETH', decimals: 18 },
  rpcUrls: {
    default: { http: ['https://sepolia.base.org'] },
  },
  blockExplorers: {
    default: { name: 'Basescan', url: 'https://sepolia.basescan.org' },
  },
});
