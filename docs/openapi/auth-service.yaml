openapi: 3.0.3
info:
  title: Vaultex Auth Service (stub)
  version: 0.0.0
  description: Target bounded context — login, register, logout, token/session bridge. See ADR-0002.
servers:
  - url: http://127.0.0.1:3001
    description: Internal (placeholder)
paths:
  /health:
    get:
      summary: Service health
      responses:
        '200':
          description: OK
  /v1/auth/login:
    post:
      summary: Login (stub — contract TBD)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                password:
                  type: string
      responses:
        '200':
          description: Authenticated
        '401':
          description: Invalid credentials
components: {}
